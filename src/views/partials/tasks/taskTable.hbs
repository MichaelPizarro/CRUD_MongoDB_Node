{{#if tasks}}
  <table class="table table-bordered table-hover table-dark">
    <thead>
      <tr>
        <th>N.</th>
        <th>Titulo</th>
        <th>Descripción</th>
        <th>Operaciones</th>
      </tr>
    </thead>
    <tbody>
      {{#each tasks}}
        <tr>
          <td>{{@index}}</td>
          <td>{{title}}</td>
          <td>{{description}}</td>
          <td>
            {{#if done}}
              <a href="/toggleDone/{{_id}}" class="btn btn-dark">No Hecho</a>
            {{else}}
              <a href="/toggleDone/{{_id}}" class="btn btn-success">Hecho</a>
            {{/if}}

            <a href="/delete/{{_id}}" class="btn btn-danger">Eliminar</a>
            <a href="/edit/{{_id}}" class="btn btn-info">Actualizar</a>
          </td>
        </tr>
      {{/each}}
    </tbody>
  </table>
{{else}}
  <div class="text-center">
    <h1>No hay Tareas</h1>
    <img src="" alt="" />
  </div>
{{/if}}